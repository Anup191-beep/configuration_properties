spring:
  datasource:
    url: jdbc:mysql://localhost:3306/myproject
    username: root
    password: anup
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
